[pytest]
DJANGO_SETTINGS_MODULE = bizpos.settings
python_files = tests.py test_*.py *_tests.py
python_classes = Test*
python_functions = test_*
addopts = 
    --verbose
    --strict-markers
    --tb=short
    --cov=apps
    --cov-report=term-missing
    --cov-report=html
    --reuse-db
markers =
    unit: Unit tests
    integration: Integration tests
    api: API endpoint tests
